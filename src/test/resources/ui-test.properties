# Application URL
app.url=http://localhost:8080/approval

# WebDriver Settings
app.browser=chrome
app.timeout.seconds=10
app.implicit.wait=5
app.explicit.wait=10

# Logging
log.level=INFO

# Hebrew Locale Support
language=he_IL