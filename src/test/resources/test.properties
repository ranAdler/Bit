# Test Environment Properties
api.base.url=http://localhost:8080
api.payment.endpoint=/api/payment/request
app.name=Bit Payment API Test
payment.endpoint=/api/payment/request

# UI Test Configuration
app.url=http://localhost:8080/approval
app.browser=CHROME
app.timeout.seconds=10

# AWS SQS Configuration
sqs.queue.url=https://sqs.region.amazonaws.com/account-id/queue-name
sqs.region=us-east-1
sqs.retry.max.attempts=3
sqs.retry.delay.ms=1000

# Logging
log.level=INFO

# Hebrew Locale Support
language=he_IL