# Production Environment Properties
api.base.url=https://api.example.com
api.payment.endpoint=/api/payment/request
app.name=Bit Payment API Production

# Payment endpoint for production environment
payment.endpoint=/api/payment/request

# AWS SQS Configuration for production
sqs.queue.url=https://sqs.region.amazonaws.com/account-id/queue-name-prod
sqs.region=us-east-1
sqs.retry.max.attempts=5
sqs.retry.delay.ms=2000

# UI Test Configuration
app.url=https://www.example.com/approval
app.browser=CHROME
app.timeout.seconds=20

# Logging
log.level=WARN